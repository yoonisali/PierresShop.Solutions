@{
  Layout = "_Layout";
}

@using PierresShop.Models

<h3>Welcome to Pierre's Sweets and Treats Shop!</h3>


<hr>
<div class="row" style="padding-top: 20px;">
<div class="col-6">
  
<h4>Flavors for @User.Identity.Name</h4>
@if (Model["flavors"].Length == 0)
{
  <p>No flavors have been added yet!</p>
} 
<ul>
  @foreach (Flavor flavors in Model["flavors"])
  {
    <li style="list-style-position: inside; padding-right: 45px;">@Html.ActionLink(@flavors.Name, "Details", "Flavors", new { id = @flavors.FlavorId})</li>
  }
</ul>
</div>

<div class="col-6">
@if (User.Identity.IsAuthenticated)
{
  <h4>Treats for @User.Identity.Name</h4>
  @if (Model["treats"].Length == 0)
  {
    <p>No treats have been added yet!</p>
  } 
  <ul>
    @foreach (Treat treat in Model["treats"])
    {
      <li style="list-style-position: inside;">@Html.ActionLink(@treat.Name, "Details", "Treats", new { id = @treat.TreatId})</li>
    }
  </ul>
}

else
{
  <h4>Treats</h4>
  <p>Please @Html.ActionLink("log in", "LogIn", "Account") to view or manage your treats and flavors.</p>
}
</div>
</div>
<p>@Html.ActionLink("Log out", "Index", "Account")</p>
<hr>

